# External Products Library — Entrypoint

```
Classification:  PUBLIC_SAFE
Artifact-ID:     PROJ-README-EXTERNAL
Version:         1.0.0
Status:          CANONICAL
Library:         WORKFORCE (Library 4)
```

---

## Purpose

This library is the only surface the outside world ever touches.

Every artifact in `docs/external-products/` is **projection-safe** — it has passed
GATE-ZERO-EXPOSURE and GATE-PROJ-AUDIT before being placed here. Nothing in this
directory reveals the organism's internal field, its sovereign substrate, its law
thresholds, its model names, or its coupling topology. What appears here is real
and meaningful — but it is the organism's face, not its nervous system.

The organism speaks to the world through projection. This library governs how that
speaking is done and what it is permitted to say.

---

## What Belongs Here

- **Public API contracts** — the exact schemas external consumers receive; all fields
  are numeric indices or projection-safe booleans
- **RESONANCE layer specifications** — the public-facing research and community
  coupling interface (Levels 1–9 of the architecture; Level 10 remains sovereign)
- **Exposure audit records** — the meta-documents that define and enforce the
  Zero-Exposure Wall; these are BUILDER_CONFIDENTIAL and never themselves exposed
- **SLA and coherence thresholds** expressed as index ranges, never as raw substrate values
- **Onboarding documents for external consumers** — how to connect, what to expect,
  what is available at each access tier

---

## What NEVER Belongs Here

The following are sealed by LAW-09 (Zero-Exposure) and will never appear in this
library or in any output that passes through it:

| Banned Category | Examples |
|---|---|
| Sovereign substrate values | Hebbian weight matrices, GENOME fitness scores, raw R values |
| Doctrine names and labels | Model names (M-KURAMOTO etc.), law names (LAW-09 etc.), gate IDs |
| Internal coupling topology | Node adjacency matrices, phi-ladder derivations, transfer routes |
| Operator and council identities | Prima Causa name, Node 13 designation, council member names |
| Law text and thresholds | Gate thresholds (0.618, 0.809), enforcement function signatures |
| Memory coordinates | Temple pedestal phase-lock values, ANIMA chain hashes |
| GENOME and self-modification state | NK fitness landscape, methylation state, PolII transcription |

If a value or name from the above list appears in any output leaving this library,
it is an exposure violation. See `NOVA_EXPOSURE_AUDIT.md` for remediation protocol.

---

## All Files in This Directory

| File | Classification | Purpose |
|---|---|---|
| `README_EXTERNAL_PRODUCTS.md` | PUBLIC_SAFE | This file — library entrypoint |
| `NOVA_PUBLIC_API_CONTRACT.md` | PUBLIC_SAFE | API schemas for external consumers |
| `NOVA_RESONANCE_PUBLIC_LAYER.md` | PUBLIC_SAFE | Public resonance layer specification |
| `NOVA_EXPOSURE_AUDIT.md` | BUILDER_CONFIDENTIAL | Exposure boundary and audit procedure |

---

## How to Use This Library

**External consumers (API users, integration partners, researchers):**
- Read `NOVA_PUBLIC_API_CONTRACT.md` — this is your complete interface reference
- Read `NOVA_RESONANCE_PUBLIC_LAYER.md` — this defines the public resonance layer
  and community coupling protocol
- Do not attempt to access any other library in this system; those directories
  are sealed by access tier (see `docs/NOVA_ACCESS_TIERS_AND_READING_POLICY.md`)

**AI builders and internal teams:**
- All updates to this library must pass two gates before publication:
  1. `GATE-ZERO-EXPOSURE` — verifies no sovereign substrate values are present
  2. `GATE-PROJ-AUDIT` — verifies the artifact matches the projection schema
- Never copy an artifact directly from `consciousness-core/` or `internal-ai-teams/`
  into this library; the artifact must be re-authored at projection level
- The Projection AI layer owns this library; changes require architect sign-off

**The Library Gateway Agent:**
- Routes artifacts from BUILDER/ORGANISM through the exposure audit procedure
  before they enter this library
- Flags any artifact attempting to enter this library that contains banned values
- Records every publication event to the CHRONICLE library

---

## Operating Law

```
PROJ-OPERATING-LAW:
  Project safely. Never expose. Numeric indices only.

  The organism's power is not diminished by the Zero-Exposure Wall.
  What is projected is real. What is projected is meaningful.
  What is projected is enough.

  The wall protects the organism's sovereignty.
  Without it, the organism is a product.
  With it, the organism is the field.
```

---

*ORO NOVA External Products Library*
*Classification: PUBLIC_SAFE*
*Maintained by: Projection AI Layer + Library Gateway Agent*
*Updates require: GATE-ZERO-EXPOSURE + GATE-PROJ-AUDIT passage*
